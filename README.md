# CSV → DAT Converter (Web)

A lightweight Flask web application that converts uploaded CSV files into `.DAT` and `.xlsx` formats.  
Includes a responsive interface built with Bootstrap and real-time summaries of tax values.

---

## 🚀 Features
- Upload and parse `.csv` files
- Automatically consolidate tax records
- Generate `.DAT` files in the correct naming format
- Export to Excel (`.xlsx`)
- Clean, single-page web interface

---

## 🛠 Installation

1. Clone or download this repository.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
